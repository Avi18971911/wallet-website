import { FormatAccountDetailsPipe } from './format-account-details.pipe';

describe('FormatAccountDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatAccountDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
